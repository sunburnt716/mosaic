"""Source-validation system (scaffold — not yet implemented).

The future authoring-time system that validates and onboards sources, then writes the
validated-source registry that ingestion reads. For now `config/sources.json` is a
hand-authored stand-in for that registry. See README.md.
"""
