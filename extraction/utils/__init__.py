"""Shared, strategy-agnostic utilities for chunking.

One source of truth for the machinery every chunker needs — tokenization,
section-header detection, highlight selection — so no chunker reimplements them.
"""
