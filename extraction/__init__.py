"""Extraction stage (scaffold — not yet implemented).

Reads `status: unprocessed` Documents from the ingestion raw store and produces
processed content (chunking, embeddings, ticker/sector enrichment). See README.md.
"""
